# Google OAuth
VITE_GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
# Client secret is required for web OAuth (not needed for iOS native)
VITE_GOOGLE_CLIENT_SECRET=your-google-client-secret
# iOS OAuth client ID (create iOS type in Google Cloud Console)
VITE_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Supabase
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# AI Configuration
# OpenAI API key is stored server-side in Supabase Edge Function secrets
# Set via: supabase secrets set OPENAI_API_KEY=your-key
# Or via Supabase Dashboard > Project Settings > Edge Functions > Secrets

# Stripe (Subscriptions)
# Publishable key is safe for client-side
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
# Price IDs from Stripe Dashboard > Products > Clarity Pro
VITE_STRIPE_PRICE_MONTHLY=price_xxx
VITE_STRIPE_PRICE_ANNUAL=price_xxx
# Secret key and webhook secret are server-side only (Edge Function secrets)
# Set via: supabase secrets set STRIPE_SECRET_KEY=sk_test_xxx
# Set via: supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_xxx
